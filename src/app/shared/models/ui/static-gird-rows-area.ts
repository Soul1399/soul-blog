export interface StaticGridRowsArea {
  minHeight: string;
  maxHeight: string | null;
  minWidth: string | null;
  maxWidth: string | null;
  overflow: string | null;
  domElement?: HTMLElement | null;
}
