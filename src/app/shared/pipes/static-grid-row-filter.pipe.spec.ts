import { StaticGridRowFilterPipe } from './static-grid-row-filter.pipe';

describe('StaticGridRowFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StaticGridRowFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
